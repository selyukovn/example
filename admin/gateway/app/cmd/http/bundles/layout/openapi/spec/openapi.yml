openapi: 3.0.0
info:
  title: "Gateway - Layout"
  version: 0.1.0
paths:

  /layout/user-info:
    $ref: "./user-info.yml"

components:
  securitySchemes:
    sessId:
      type: http
      scheme: bearer

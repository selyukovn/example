
За экспорт событий отвечает `kafka-connect` с `debezium`-коннектором.

Код в `kafapi` -- это аналог `pb` или `openapi` кода/спецификации,
который копируется (при отсутствии общего репозитория)
в сервисы-приемники для упрощения обработки сообщений.

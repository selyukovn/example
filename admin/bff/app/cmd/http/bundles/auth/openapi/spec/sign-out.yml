delete:
  security:
    - sessId: [ ]

  responses:
    "200":
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              redirect_url:
                type: string
                example: "/auth/sign-in/welcome"
    "422":
      $ref: "./common/expected-error.yml"

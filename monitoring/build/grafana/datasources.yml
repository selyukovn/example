apiVersion: 1

datasources:
  - name: 'prometheus'
    type: 'prometheus'
    access: 'proxy'
    url: 'http://monitoring-prometheus:9090'
    editable: false

  - name: 'loki'
    type: 'loki'
    access: 'proxy'
    url: 'http://monitoring-loki:3100'
    editable: false
